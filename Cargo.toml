[package]
name = "fetrust"
version = "0.1.5"
edition = "2021"
description = "fetchtool in rust"
categories = ["os","fetching","system","device"]
license = "GPL-3.0"

[profile.release]
strip = true
opt-level = 3
lto = true
debug = false
codegen-units = 1

[dependencies]
